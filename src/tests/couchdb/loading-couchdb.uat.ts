import { describe, it } from 'vitest'

describe('loading config from couchdb', function () {
  it.skip('should be able to load config from a couchdb document', function () {
    // Given
    // When
    // Then
  })
})