import { describe, it } from "vitest";

describe("loading config from couchdb", () => {
	it.skip("should be able to load config from a couchdb document", () => {
		// Given
		// When
		// Then
	});
});
