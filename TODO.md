* make a couchdb source
* make the alias system work
* make doc gen
* make custom schema type such as `port`, and `duration`
* remove the files from the file source constructor
* add a deprecation system à la C#
* add a feature to build the json schema from a config schema
* add a cli to bootstrap a config with all the defaults
* add a cache system