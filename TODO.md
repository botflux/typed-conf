* vault
  * test what happens in case vault respond with a wrong schema
* type system
  * implement an array type
  * implement an union type
  * implement a fat union type
  * implement string with special format (it could be regex, but I don't know if the error messages are going to be clear)
  * implement a merge type
* multi tenant
  * implement multi tenant versions of the sources
